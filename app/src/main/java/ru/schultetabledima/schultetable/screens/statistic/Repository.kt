package ru.schultetabledima.schultetable.screens.statistic

import javax.inject.Inject


class Repository @Inject constructor() {

//    suspend fun getList(): List<Result> {
////        val db = App.getInstance().database
////        val resultDao = db.resultDao()
////        return resultDao.getAll(ConstructQuery.getQuery(quantityTables, valueType, playedSizes)) ?: emptyList()
//    }
}